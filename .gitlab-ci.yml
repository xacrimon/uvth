image: 'rust:slim'

stages:
  - test

variables:
  CARGO_HOME: $CI_PROJECT_DIR/cargo
  RUSTUP_HOME: $CI_PROJECT/rustup

test:
  stage: test
  script:
    - rustc --version
    - cargo --version
    - cargo test --verbose

cache:
  paths:
    - cargo/
    - target/
    - rustup/
